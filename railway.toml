[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.webapp"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.healthcheck]]
type = "http"
path = "/api/config"
timeout = 60
interval = 10
